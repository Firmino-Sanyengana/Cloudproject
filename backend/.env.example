DATABASE_URL="file:./dev.db"
JWT_SECRET="troque-este-segredo-em-producao"
JWT_EXPIRES_IN="7d"
PORT=4000
CORS_ORIGIN=http://localhost:4001

# Email (opcional). Se vazio, os códigos aparecem no console do servidor.
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Sofulano Ukulondja <no-reply@sofulano.local>"
